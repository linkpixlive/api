# Documentação para IA — AGENTS.md como sumário + docs temáticos (versão resumida ao máximo)

## Objetivo
Substituir o AGENTS.md atual (320 linhas desatualizadas, sempre em contexto) por um **sumário enxuto em PT-BR** apontando para 6 docs detalhados, escritos a partir do código real (já verificado por exploração). **Diretiva do usuário: resumir ao máximo** — cada doc em ~50–90 linhas, formato denso (tabelas e bullets, sem prosa), um fato em um único lugar.

## Arquivos

1. **`AGENTS.md`** (~50–60 linhas): identidade (LinkPix, ex-Tipply; NestJS 11, Prisma 7/Postgres, Redis, BullMQ, Socket.IO) + bloco "Invariantes" (~8 linhas) + tabela de ponteiros com gatilho de leitura + seção artefatos + seção "Agent skills" preservada.
2. **`CONTEXT.md`** (~70 linhas): produto, mapa dos 14 módulos (1 linha cada), fluxo da doação ponta a ponta, ciclo de saque, glossário mínimo, implementado vs planejado.
3. **`docs/architecture.md`** (~80 linhas): camadas e convenções verificadas (controllers thin, repositories com DTOs em interfaces TS e DbModule global, entities @Expose, os 4 Contracts, envelope exato de resposta, guards globais, exceções documentadas, verificação = pnpm build + lint).
4. **`docs/data.md`** (~80 linhas): Prisma 7 (migrations, 12 models, 10 enums em tabela), invariantes financeiras (ledger, trigger, FOR UPDATE), Redis (API do RedisService + tabela de chaves com as 2 não documentadas: `auth:session`, `auth:user_sessions`).
5. **`docs/realtime.md`** (~80 linhas): BullMQ + pipeline donations-queue com gotchas (IA comentada, `.wav`, BUCKET_URL), email-queue, gateways overlay/dashboard, motor da fila de alertas (dispatch gate, SETNX, pause/skip/test/replay).
6. **`docs/security.md`** (~80 linhas): env fail-fast, cripto, JWT + sessões Redis, 2FA/OTP, 12 throttlers, sanitização, webhook HMAC (gotcha), dev força Pix efipay@sejaefi.com.br.
7. **`docs/agents/artifacts.md`** (~40 linhas): convenção canônica de artefatos — `docs/plans/`, `docs/specs/`, `docs/adr/`, `docs/audits/`; naming `YYYY-MM-DD-<slug>.md`; vale para qualquer ferramenta (ZCode, Claude, opencode…); caches `.zcode/`/`.worktrees/` são transitórios.

## Migração (git mv)
- `docs/superpowers/specs/*` → `docs/specs/`; `docs/superpowers/plans/*` → `docs/plans/`; remover `docs/superpowers/`.
- 5 relatórios `docs/*-vulnerabilities.md` → `docs/audits/` (raiz de docs fica só com pastas).

## Correções vs AGENTS.md atual
TTS é Gradium (não Google) e `.wav`; JWT `{sub, sid, roles}` com expiração via env; sessões revogáveis em Redis; moderação IA comentada no processor; repos são classes concretas em módulo global; Prisma 7; ThrottlerGuard off em dev; produto = LinkPix.

## Passos
1. Migração git mv (acima).
2. Re-ler fontes densos (app.module, redis-keys, envelope, processor, schema, .env.example) para precisão.
3. Escrever os 6 docs + reescrever AGENTS.md.
4. Verificação: ponteiros resolvem; sem duplicação entre docs; grep anti-sedimento (`.mp3`, "Google TTS"); AGENTS.md ≤ 60 linhas.